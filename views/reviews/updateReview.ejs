<%- layout('/layouts/boilerplate.ejs') %>

<form action="/listings/<%= id %>/review/<%= review._id %>?_method=PATCH" method="post" class="needs-validation" novalidate>
    <h4>Edit Review</h4>
    <div class="form-div">
        <label for="comment" class="form-label">Comment:</label>
        <textarea name="review[comment]" id="comment" class="form-control" placeholder="type a comment" required><%= review.comment %></textarea>
        <div class="valid-feedback"></div>
        <div class="invalid-feedback">Comment is required</div>
    </div>
        <!-- <input type="range" min="1" max="5" class="form-range" name="review[rating]" id="rating" required> -->
    <!-- STARABILITY HTML -->
     <div>
         <label for="rating" class="form-label">Rate:</label>
    <fieldset class="starability-slot"id="rating" required>
        <input type="radio" id="no-rate" class="input-no-rate" name="review[rating]" value="1" checked aria-label="No rating." <%= review.rating == 1 ? 'checked' : '' %> />
        <input type="radio" id="first-rate1" name="review[rating]" value="1" <%= review.rating == 1 ? 'checked' : '' %> />
        <label for="first-rate1" title="Terrible">1 star</label>
        <input type="radio" id="first-rate2" name="review[rating]" value="2" <%= review.rating == 2 ? 'checked' : '' %> />
        <label for="first-rate2" title="Not good">2 stars</label>
        <input type="radio" id="first-rate3" name="review[rating]" value="3" <%= review.rating == 3 ? 'checked' : '' %> />
        <label for="first-rate3" title="Average">3 stars</label>
        <input type="radio" id="first-rate4" name="review[rating]" value="4" <%= review.rating == 4 ? 'checked' : '' %> />
        <label for="first-rate4" title="Very good">4 stars</label>
        <input type="radio" id="first-rate5" name="review[rating]" value="5" <%= review.rating == 5 ? 'checked' : '' %> />
        <label for="first-rate5" title="Amazing">5 stars</label>
        <div class="valid-feedback"></div>
        <div class="invalid-feedback"></div>
    </fieldset>
    </div>
    <button class="btn btn-danger text-light">Add</button>
</form>